// src/pages/teacher/AiGenerate.jsx
// TODO: AI question generation page
// - Upload PDF/Word file (drag-and-drop)
// - Select subject, difficulty level, number of questions to generate
// - Preview generated questions, allow editing before saving to bank
// - Show loading/progress while Gemini API processes

const AiGenerate = () => {
  // TODO: useState for file, settings, generatedQuestions
  // TODO: handleUpload → call questionService.generateAiQuestions()
  // TODO: handleSave → save approved questions to bank

  return (
    <div>
      <h1>Tạo câu hỏi bằng AI</h1>
      {/* TODO: Build file upload and preview UI */}
    </div>
  );
};

export default AiGenerate;
