// src/pages/teacher/ResultView.jsx
// TODO: View detailed results for a specific exam (teacher's perspective)
// - Summary: pass rate, average score, score distribution chart
// - Per-student result table with download CSV option
// - Highlight weak students (below passing threshold)

const ResultView = () => {
  // TODO: useParams for examId
  // TODO: useEffect → fetch exam results

  return (
    <div>
      <h1>Kết quả kỳ thi</h1>
      {/* TODO: Build analytics charts and results table */}
    </div>
  );
};

export default ResultView;
