// src/pages/teacher/ExamManager.jsx
// TODO: Exam management page for teachers
// - List of created exams with status (draft/published/closed)
// - Create new exam: select questions from bank, set duration, assign to class
// - View detailed results for each exam (link to ResultView)

const ExamManager = () => {
  // TODO: useEffect → fetch teacher's exams

  return (
    <div>
      <h1>Quản lý đề thi</h1>
      {/* TODO: Build exam list & create form UI */}
    </div>
  );
};

export default ExamManager;
