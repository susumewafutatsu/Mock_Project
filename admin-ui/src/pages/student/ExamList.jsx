// src/pages/student/ExamList.jsx
// TODO: List of available exams for student
// - Show exam name, subject, duration, scheduled time, status (upcoming/ongoing/done)
// - "Vào thi" button for active exams → navigate to ExamRoom
// - "Xem kết quả" for completed exams → navigate to ResultHistory

const ExamList = () => {
  // TODO: useEffect → fetch examService.getExams()

  return (
    <div>
      <h1>Danh sách kỳ thi</h1>
      {/* TODO: Build exam list UI with status badges */}
    </div>
  );
};

export default ExamList;
