// src/pages/student/ResultHistory.jsx
// TODO: Student's personal result history
// - Table of past exams: name, score, grade, date
// - Score trend line chart over time
// - Detailed view: which questions were wrong

const ResultHistory = () => {
  // TODO: useEffect → fetch examService.getStudentResults()

  return (
    <div>
      <h1>Lịch sử kết quả</h1>
      {/* TODO: Build history table and trend chart */}
    </div>
  );
};

export default ResultHistory;
