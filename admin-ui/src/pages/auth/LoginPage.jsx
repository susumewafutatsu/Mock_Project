// src/pages/auth/LoginPage.jsx
// TODO: Implement login page UI
// - Email/password form with validation
// - Google SSO button (redirect to backend OAuth2 endpoint)
// - On success: save token, redirect based on role (ADMIN/TEACHER/STUDENT)
// - Link to register page

const LoginPage = () => {
  // TODO: useAuth() for login function
  // TODO: useState for form fields
  // TODO: handleSubmit → call authService.login(), then navigate

  return (
    <div>
      <h1>Đăng nhập</h1>
      {/* TODO: Build login form UI */}
    </div>
  );
};

export default LoginPage;
