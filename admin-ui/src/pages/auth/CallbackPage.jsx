// src/pages/auth/CallbackPage.jsx
// TODO: Handle redirect from Google OAuth2
// - Read token from URL query params (?token=...)
// - Save token to localStorage via AuthContext
// - Redirect to correct dashboard based on role

const CallbackPage = () => {
  // TODO: useEffect on mount → parse URL, call setCurrentUser, navigate

  return <div>Đang xử lý đăng nhập...</div>;
};

export default CallbackPage;
