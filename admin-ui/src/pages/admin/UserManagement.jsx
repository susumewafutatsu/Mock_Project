// src/pages/admin/UserManagement.jsx
// TODO: User management page for admin
// - Table of all users with role, email, status
// - Filter by role (ADMIN / TEACHER / STUDENT)
// - Change user role action
// - Delete user action

const UserManagement = () => {
  // TODO: useEffect → fetch users
  // TODO: Handle role update, delete

  return (
    <div>
      <h1>Quản lý người dùng</h1>
      {/* TODO: Build user table UI */}
    </div>
  );
};

export default UserManagement;
