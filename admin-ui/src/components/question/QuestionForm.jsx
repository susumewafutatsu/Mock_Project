// src/components/question/QuestionForm.jsx
// TODO: Form to create or edit a question
// Props: initialData (for edit), onSubmit (fn), onCancel (fn)
// Fields: content, questionType, difficulty (1-5), answers (array), correctIndex

const QuestionForm = ({ initialData, onSubmit, onCancel }) => {
  // TODO: useState for form fields
  // TODO: Dynamic answer input rows (add/remove)
  // TODO: Validate at least 2 answers and one marked correct

  return (
    <form>
      {/* TODO: Build question creation form */}
    </form>
  );
};

export default QuestionForm;
