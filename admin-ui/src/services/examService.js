// src/services/examService.js
// TODO: Implement API calls for exams
// - getExams()                         → GET /student/exams
// - startExam(examId)                  → GET /student/exams/{examId}/start
// - submitExam(examId, answers)        → POST /student/exams/{examId}/submit
// - getStudentResults()                → GET /student/results
// - createExam(data)                   → POST /teacher/exams
// - getExamResults(examId)             → GET /teacher/exams/{examId}/results

import api from './api';

// TODO: Implement and export functions
